<Window x:Class="Pankha.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pankha.UI"
        mc:Ignorable="d"
        Title="Pankha Agent Configuration" Height="450" Width="600"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Padding" Value="10" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="3" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Pankha Agent Configuration" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Agent Settings -->
                <GroupBox Header="Agent Settings">
                    <StackPanel>
                        <TextBlock Text="Agent Name:" />
                        <TextBox Text="{Binding AgentName}" />
                        
                        <TextBlock Text="Agent ID:" />
                        <TextBox Text="{Binding AgentId}" IsReadOnly="True" Background="#F0F0F0" />
                    </StackPanel>
                </GroupBox>

                <!-- Backend Connection -->
                <GroupBox Header="Backend Connection">
                    <StackPanel>
                        <TextBlock Text="Backend URL:" />
                        <TextBox Text="{Binding BackendUrl}" />
                        
                        <TextBlock Text="Reconnect Interval (ms):" />
                        <TextBox Text="{Binding ReconnectInterval}" />
                    </StackPanel>
                </GroupBox>

                <!-- Hardware Control -->
                <GroupBox Header="Hardware Control">
                    <StackPanel>
                        <CheckBox Content="Enable Fan Control" IsChecked="{Binding EnableFanControl}" Margin="0,0,0,10" />
                        
                        <TextBlock Text="Update Interval (sec):" />
                        <TextBox Text="{Binding UpdateInterval}" />
                        
                        <TextBlock Text="Emergency Temperature (°C):" />
                        <TextBox Text="{Binding EmergencyTemp}" />
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer / Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusColor}" VerticalAlignment="Center" Margin="0,0,20,0" />
            <Button Content="Cancle" Command="{Binding CancelCommand}" Width="80" Margin="0,0,10,0" />
            <Button Content="Save Settings" Command="{Binding SaveCommand}" Width="100" />
        </StackPanel>
    </Grid>
</Window>
