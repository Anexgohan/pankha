{
  "name": "pankha",
  "version": "1.0.0",
  "description": "Self-hosted Docker app with web GUI",
  "license": "AGPL-3.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "POSTGRES_HOST=localhost LOG_LEVEL=warn npm run dev --workspace=backend",
    "build": "concurrently \"npm run build --workspace=frontend\" \"npm run build --workspace=backend\"",
    "test": "npm run test --workspace=frontend && npm run test --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "typecheck": "npm run typecheck --workspace=frontend && npm run typecheck --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
